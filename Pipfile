# Present only to pin the Vercel Python runtime. Dependencies are still
# installed from requirements.txt; Python 3.12 has prebuilt wheels for
# pydantic-core (3.14 would force a Rust source build that fails).
[requires]
python_version = "3.12"
